name: My Deploy

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to production
        uses: ./.github/workflows/main.yml
        with:
          service-id: ${{ https://api.render.com/deploy/srv-clghhref27hc739kkt70?key=ekj4OwKL_3s }}
          api-key: ${{ rnd_opHPVmdaY46FRfLnWlcghqzSpV8B }}
